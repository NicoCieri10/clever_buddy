import 'package:flutter/material.dart';

class QuickSessionWidget extends StatelessWidget {
  const QuickSessionWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      children: [
        Spacer(),
        Text('Quick Session'),
        Spacer(),
      ],
    );
  }
}
